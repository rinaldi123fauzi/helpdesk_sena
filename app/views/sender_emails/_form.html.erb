<%= form_with(model: sender_email, local: true) do |form| %>
  <% if sender_email.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sender_email.errors.count, "error") %> prohibited this sender_email from being saved:</h2>

      <ul>
      <% sender_email.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email_to %>
    <%= form.text_field :email_to %>
  </div>

  <div class="field">
    <%= form.label :parent_id %>
    <%= form.number_field :parent_id %>
  </div>

  <div class="field">
    <%= form.label :token %>
    <%= form.number_field :token %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
